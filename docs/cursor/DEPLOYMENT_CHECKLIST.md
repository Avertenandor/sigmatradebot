# ✅ Чеклист деплоя SigmaTrade Bot

**Дата:** 2025-01-16  
**Для:** Cursor IDE + Ручная проверка  
**Цель:** Контроль качества перед production

---

## 📋 ПЕРЕД НАЧАЛОМ РАБОТЫ

- [ ] Cursor IDE открыт с проектом
- [ ] Git настроен и работает
- [ ] Есть доступ к серверу через gcloud/ssh
- [ ] Прочитаны документы:
  - [ ] `CURSOR_INSTRUCTIONS_SERVER_CLEANUP.md`
  - [ ] `QUICK_START_GUIDE.md`
  - [ ] `COMMANDS_CHEATSHEET.md`

---

## 🔴 P0 - КРИТИЧЕСКИЕ ЗАДАЧИ

### 1. Валидация переменных окружения

**Файл:** `scripts/validate-env.py`

- [ ] Добавлена функция `validate_telegram_token()`
- [ ] Добавлена функция `validate_wallet_address()`
- [ ] Добавлена функция `validate_database_url()`
- [ ] Функции используются в `validate_env()`
- [ ] Скрипт тестирован локально
- [ ] Все проверки проходят

**Тест:**
```bash
python3 scripts/validate-env.py
# Должен вывести все проверки с ✅
```

---

### 2. Health Check скрипт

**Файл:** `scripts/health-check.sh`

- [ ] Файл создан
- [ ] Права установлены (chmod +x)
- [ ] Проверка Docker контейнеров работает
- [ ] Проверка PostgreSQL работает
- [ ] Проверка Redis работает
- [ ] Проверка логов на ошибки работает
- [ ] Проверка места на диске работает
- [ ] Скрипт тестирован

**Тест:**
```bash
chmod +x scripts/health-check.sh
./scripts/health-check.sh
# Должен вывести все проверки
```

---

### 3. Валидация в Settings

**Файл:** `app/config/settings.py`

- [ ] Импорты добавлены (`field_validator`, `model_validator`, `re`)
- [ ] Валидатор `telegram_bot_token` добавлен
- [ ] Валидатор `wallet_address` добавлен
- [ ] Валидатор `system_wallet_address` добавлен
- [ ] Валидатор `database_url` добавлен
- [ ] Валидатор `secret_key` и `encryption_key` добавлен
- [ ] Model validator `validate_production` добавлен
- [ ] Улучшена функция `get_admin_ids()` с обработкой ошибок

**Тест:**
```python
python3 -c "from app.config.settings import Settings; print('OK')"
```

---

### 4. Docker Entrypoint

**Файл:** `docker-entrypoint.sh`

- [ ] Функция `wait_for_postgres()` добавлена
- [ ] Функция `wait_for_redis()` добавлена
- [ ] Проверка `DATABASE_URL` добавлена
- [ ] Проверка `TELEGRAM_BOT_TOKEN` добавлена
- [ ] Вызовы функций ожидания добавлены
- [ ] Обработка ошибок миграций улучшена
- [ ] Логирование для каждой команды добавлено

**Тест:**
```bash
# Проверить синтаксис
bash -n docker-entrypoint.sh
```

---

### 5. Обновление .env.example

**Файл:** `.env.example`

- [ ] Предупреждения в начале файла добавлены
- [ ] Комментарии к критичным полям добавлены
- [ ] Примеры форматов для токенов и адресов добавлены
- [ ] Инструкции по генерации ключей добавлены
- [ ] Описание каждой переменной понятно

---

### 6. Улучшение обработки ошибок

**Файл:** `bot/main.py`

- [ ] Глобальный обработчик ошибок `error_handler()` добавлен
- [ ] Логирование ошибок с полным traceback настроено
- [ ] Уведомления админам о критических ошибках настроены
- [ ] Обработчик зарегистрирован в dispatcher

---

## 🟡 P1 - ВАЖНЫЕ ЗАДАЧИ

### 7. Unit тесты

**Файл:** `tests/unit/test_settings.py`

- [ ] Файл создан
- [ ] Тест `test_invalid_telegram_token()` создан
- [ ] Тест `test_invalid_wallet_address()` создан
- [ ] Тест `test_debug_not_allowed_in_production()` создан
- [ ] Тест `test_weak_password_not_allowed()` создан
- [ ] Все тесты проходят

**Тест:**
```bash
pytest tests/unit/test_settings.py -v
```

---

### 8. Документация

**Файл:** `DEPLOY_TO_SERVER.md`

- [ ] Раздел "Типичные проблемы" добавлен
- [ ] Решения для каждой проблемы описаны
- [ ] Команды диагностики добавлены
- [ ] Примеры исправления добавлены

---

## 🔍 ПРОВЕРКА КАЧЕСТВА

### Проверка кода

```bash
# Все файлы из задач существуют
ls -la scripts/validate-env.py
ls -la scripts/health-check.sh
ls -la app/config/settings.py
ls -la bot/main.py
ls -la docker-entrypoint.sh
ls -la .env.example

# Python файлы корректны
python3 -m py_compile app/config/settings.py
python3 -m py_compile bot/main.py
python3 -m py_compile scripts/validate-env.py

# Bash скрипты корректны
bash -n scripts/health-check.sh
bash -n docker-entrypoint.sh

# Скрипты исполняемые
[ -x scripts/health-check.sh ] && echo "OK" || echo "FAIL"
[ -x scripts/server-deploy.sh ] && echo "OK" || echo "FAIL"
```

---

### Проверка Docker

```bash
# Собрать образ
docker build -f Dockerfile.python -t sigmatrade:test .

# Проверить что образ работает
docker run --rm sigmatrade:test python -c "import app; import bot; print('OK')"

# Проверить docker-compose
docker-compose -f docker-compose.python.yml config
```

---

### Проверка тестов

```bash
# Все тесты проходят
pytest tests/ -v

# Покрытие тестами
pytest --cov=app --cov=bot tests/
```

---

## 📦 GIT

### Коммит изменений

```bash
# Статус
git status

# Добавить все изменения
git add .

# Проверить что добавлено
git diff --staged

# Коммит
git commit -m "chore: production deployment preparation

- Enhanced validate-env.py with format validators
- Added health-check.sh for production monitoring
- Improved settings.py with comprehensive validation
- Better error handling in bot/main.py
- Enhanced docker-entrypoint.sh with wait functions
- Updated .env.example with detailed comments
- Added unit tests for settings validation
- Updated deployment documentation

P0 tasks completed:
- ✅ Environment validation
- ✅ Health check script
- ✅ Settings validators
- ✅ Error handlers
- ✅ Docker improvements

P1 tasks completed:
- ✅ Unit tests
- ✅ Documentation updates"

# Пуш
git push origin claude/sigmatradebot-python-migration-01UUhWd7yPartmZdGxtPAFLo
```

---

## 🚀 ДЕПЛОЙ

### Подготовка

- [ ] Все чеклисты выше выполнены
- [ ] Все тесты проходят
- [ ] Код закоммичен и запушен
- [ ] Документация актуальна

### На сервере

```bash
# Подключение
gcloud compute ssh sigmatrade-20251108-210354 --zone=europe-north1-a

# Или
ssh konfu@34.88.234.78

# Клонирование
sudo mkdir -p /opt/sigmatradebot
sudo chown -R $USER:$USER /opt/sigmatradebot
cd /opt/sigmatradebot
git clone -b claude/sigmatradebot-python-migration-01UUhWd7yPartmZdGxtPAFLo \
  https://github.com/Avertenandor/sigmatradebot.git .

# Деплой
chmod +x scripts/*.sh
./scripts/server-deploy.sh
```

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

### После деплоя

- [ ] Health check проходит: `./scripts/health-check.sh`
- [ ] Все контейнеры запущены: `docker-compose -f docker-compose.python.yml ps`
- [ ] Нет ошибок в логах: `docker-compose -f docker-compose.python.yml logs --tail=100`
- [ ] PostgreSQL работает: `docker exec sigmatrade-postgres pg_isready`
- [ ] Redis работает: `docker exec sigmatrade-redis redis-cli ping`
- [ ] База данных создана: `docker exec sigmatrade-postgres psql -U sigmatrade -l`
- [ ] Миграции применены: `docker exec sigmatrade-bot alembic current`

### Тестирование бота

- [ ] Бот отвечает в Telegram
- [ ] `/start` работает
- [ ] Регистрация пользователя работает
- [ ] Главное меню отображается
- [ ] Нет ошибок при навигации

---

## 📊 МЕТРИКИ

### После 24 часов работы

- [ ] Логи проверены (нет критических ошибок)
- [ ] Backup создан автоматически
- [ ] Health check проходит
- [ ] Место на диске ОК (< 80%)
- [ ] Пользователи могут регистрироваться
- [ ] Депозиты создаются (если тестировали)

---

## 🎯 ИТОГОВЫЙ СТАТУС

**Дата завершения:** __________

### Код
- [ ] Все P0 задачи выполнены
- [ ] Все P1 задачи выполнены
- [ ] Тесты написаны и проходят
- [ ] Код закоммичен

### Сервер
- [ ] Сервер настроен
- [ ] Docker работает
- [ ] База данных создана
- [ ] Бот запущен

### Проверка
- [ ] Health check проходит
- [ ] Бот отвечает в Telegram
- [ ] Нет критических ошибок
- [ ] Документация актуальна

---

## 📝 NOTES

**Проблемы при деплое:**
_________________________
_________________________
_________________________

**Решения:**
_________________________
_________________________
_________________________

**Дополнительные задачи:**
_________________________
_________________________
_________________________

---

**СТАТУС:** 

- [ ] ❌ Не начат
- [ ] 🟡 В процессе
- [ ] ✅ Завершен
- [ ] 🎉 Production ready!

---

**Подпись:** __________  
**Дата:** __________

