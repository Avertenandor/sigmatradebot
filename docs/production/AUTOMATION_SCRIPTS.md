# 🤖 Automation Scripts for Production Deployment

Автоматизированные скрипты для упрощения деплоя и управления ботом.

---

## 📋 Доступные скрипты

### 1. `scripts/setup-env.sh` - Настройка окружения

**Назначение:** Автоматическое создание и настройка `.env` файла.

**Использование:**
```bash
chmod +x scripts/setup-env.sh
./scripts/setup-env.sh
```

**Что делает:**
- Создает `.env` из `.env.example` (если не существует)
- Генерирует `SECRET_KEY` и `ENCRYPTION_KEY` автоматически
- Устанавливает правильные права доступа (600)
- Проверяет наличие всех обязательных переменных
- Выводит список переменных, которые нужно заполнить

**Пример вывода:**
```
✅ .env file created from .env.example
✅ Generated SECRET_KEY
✅ Generated ENCRYPTION_KEY
✅ Set .env file permissions to 600
⚠️ Variables with placeholder values:
  - TELEGRAM_BOT_TOKEN
  - DATABASE_URL
  - WALLET_PRIVATE_KEY
```

---

### 2. `scripts/validate-env.py` - Валидация окружения

**Назначение:** Проверка корректности всех переменных окружения.

**Использование:**
```bash
python3 scripts/validate-env.py
```

**Что проверяет:**
- Все обязательные переменные установлены
- Нет placeholder значений
- Формат адресов кошельков (начинаются с 0x)
- Формат RPC URL (HTTP/HTTPS)
- Формат DATABASE_URL (PostgreSQL)
- Наличие админов
- Корректность настроек Redis

**Пример вывода:**
```
🔍 Validating environment variables...

✅ All environment variables are valid!
Environment is ready for deployment.
```

---

### 3. `scripts/check-readiness.sh` - Проверка готовности

**Назначение:** Комплексная проверка готовности к production деплою.

**Использование:**
```bash
chmod +x scripts/check-readiness.sh
./scripts/check-readiness.sh
```

**Что проверяет:**
1. ✅ Наличие `.env` файла
2. ✅ Валидность переменных окружения
3. ✅ Установлен Docker
4. ✅ Установлен Docker Compose
5. ✅ Наличие Dockerfile.python
6. ✅ Наличие docker-compose.python.yml
7. ✅ Настройка DATABASE_URL
8. ✅ Наличие всех необходимых скриптов

**Пример вывода:**
```
✅ All checks passed! Bot is ready for production deployment.

Next steps:
1. Review .env file: nano .env
2. Run deployment: ./scripts/deploy-python.sh production
```

---

### 4. `scripts/quick-deploy.sh` - Быстрый деплой

**Назначение:** Упрощенный скрипт для быстрого деплоя.

**Использование:**
```bash
chmod +x scripts/quick-deploy.sh
./scripts/quick-deploy.sh
```

**Что делает:**
1. Проверяет готовность (через `check-readiness.sh`)
2. Настраивает окружение (если нужно)
3. Валидирует переменные
4. Собирает Docker образы
5. Запускает контейнеры
6. Проверяет статус деплоя

**Время выполнения:** ~5-10 минут

---

### 5. `scripts/deploy-python.sh` - Полный деплой

**Назначение:** Полнофункциональный скрипт деплоя с тестами и проверками.

**Использование:**
```bash
chmod +x scripts/deploy-python.sh
./scripts/deploy-python.sh production
```

**Что делает:**
- Pre-flight checks
- Установка зависимостей
- Запуск тестов (для production)
- Сборка Docker образа
- Деплой через Docker Compose
- Post-deployment verification

---

## 🚀 Быстрый старт

### Минимальный деплой (30-60 минут):

```bash
# 1. Настроить окружение
./scripts/setup-env.sh
nano .env  # Заполнить все переменные

# 2. Проверить готовность
./scripts/check-readiness.sh

# 3. Быстрый деплой
./scripts/quick-deploy.sh
```

### Полный деплой (2-4 часа):

```bash
# 1. Настроить окружение
./scripts/setup-env.sh
nano .env

# 2. Проверить готовность
./scripts/check-readiness.sh

# 3. Полный деплой с тестами
./scripts/deploy-python.sh production
```

---

## 📊 Workflow

```
┌─────────────────┐
│ setup-env.sh    │ → Создает .env, генерирует секреты
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ validate-env.py │ → Проверяет корректность переменных
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│check-readiness  │ → Комплексная проверка готовности
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ quick-deploy.sh │ → Быстрый деплой
│  или            │
│ deploy-python.sh│ → Полный деплой
└─────────────────┘
```

---

## 🔧 Интеграция с Docker

Все скрипты интегрированы с Docker:

- `docker-entrypoint.sh` автоматически валидирует окружение при старте
- `docker-compose.python.yml` использует `.env` файл
- Все скрипты работают как внутри, так и вне Docker контейнеров

---

## 📝 Примечания

1. **Права доступа:** Все `.sh` скрипты должны быть исполняемыми:
   ```bash
   chmod +x scripts/*.sh
   ```

2. **Python скрипты:** Требуют Python 3.11+ и установленных зависимостей проекта

3. **Безопасность:** `.env` файл автоматически получает права 600 (только владелец)

4. **Логирование:** Все скрипты выводят цветной вывод для удобства

---

## 🔗 Связанные документы

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по деплою
- [DEPLOY_CHECKLIST.md](DEPLOY_CHECKLIST.md) - Чеклист деплоя
- [REMAINING_TASKS.md](REMAINING_TASKS.md) - Оставшиеся задачи

