# 🎯 НАСТРОЙКА СЕРВЕРА SIGMATRADE - ЗАВЕРШЕНО ✅

## 📋 КРАТКАЯ ИНФОРМАЦИЯ

| Параметр | Значение |
|----------|----------|
| **Имя сервера** | sigmatrade-20251108-210354 |
| **Внешний IP** | 34.88.234.78 |
| **Внутренний IP** | 10.166.0.3 |
| **Зона** | europe-north1-a |
| **Проект** | telegram-bot-444304 |
| **ОС** | Debian 12 (bookworm) |
| **Тип машины** | e2-medium |
| **Uptime** | 1 день+ |
| **Свободно на диске** | 6.5 GB |

---

## ✅ ЧТО УЖЕ СДЕЛАНО

1. ✅ **Service Account активирован**
   - Аккаунт: `claude-admin-528@telegram-bot-444304.iam.gserviceaccount.com`
   - Ключ: `telegram-bot-444304-2808e7f2ef6c.json`

2. ✅ **SSH подключение настроено**
   - Конфиг: `C:\Users\konfu\.ssh\config`
   - 2 варианта подключения (IAP туннель и прямой)

3. ✅ **Docker установлен**
   - Docker: v28.5.2
   - Docker Compose: v2.40.3
   - Пользователь konfu в группе docker

4. ✅ **Структура директорий создана**
   ```
   /opt/sigmatrade/
   ├── logs/      (логи бота)
   ├── data/      (данные и БД)
   ├── backups/   (резервные копии)
   └── README.md  (инструкции на сервере)
   ```

---

## 🔌 ПОДКЛЮЧЕНИЕ ЧЕРЕЗ CURSOR IDE

### Шаг 1: Открыть Cursor
Запустите Cursor IDE

### Шаг 2: Вызвать команду подключения
- Нажмите **F1**
- Введите: `Remote-SSH: Connect to Host`
- Нажмите Enter

### Шаг 3: Выбрать сервер
Выберите один из вариантов:

#### Вариант 1: **sigmatrade** (через IAP туннель - безопаснее)
- Использует Google Cloud IAP
- Не требует открытых портов
- Медленнее на ~100-200ms

#### Вариант 2: **sigmatrade-direct** (прямое подключение - быстрее)
- Напрямую по IP: 34.88.234.78
- Быстрее отклик
- Требует открытый порт 22

### Шаг 4: Открыть проект
После подключения:
1. File → Open Folder
2. Выберите `/opt/sigmatrade`
3. Нажмите OK

---

## 📡 КОМАНДЫ ДЛЯ POWERSHELL

### Подключиться по SSH
```powershell
# Способ 1: Через gcloud
gcloud compute ssh sigmatrade-20251108-210354 --zone=europe-north1-a

# Способ 2: Через ssh (после настройки config)
ssh sigmatrade
# или
ssh sigmatrade-direct
```

### Проверить статус сервера
```powershell
gcloud compute instances list --filter="name=sigmatrade-20251108-210354"
```

### Остановить сервер
```powershell
gcloud compute instances stop sigmatrade-20251108-210354 --zone=europe-north1-a
```

### Запустить сервер
```powershell
gcloud compute instances start sigmatrade-20251108-210354 --zone=europe-north1-a
```

---

## 🐳 РАБОТА С DOCKER НА СЕРВЕРЕ

После подключения к серверу:

### Проверка Docker
```bash
docker --version
docker compose version
docker ps
```

### Запуск бота (когда будет готов)
```bash
cd /opt/sigmatrade
docker compose up -d
```

### Просмотр логов
```bash
docker compose logs -f
```

### Остановка
```bash
docker compose down
```

### Перезапуск
```bash
docker compose restart
```

---

## 📁 СЛЕДУЮЩИЕ ШАГИ

### ⏳ Ожидаются от пользователя:
1. **Директория проекта** - где находится код нового бота
2. **Репозиторий** - GitHub/GitLab URL для клонирования
3. **Конфигурация** - токены, API ключи, настройки

### После получения:
1. Клонировать репозиторий в `/opt/sigmatrade/`
2. Настроить `.env` файл с секретами
3. Создать `docker-compose.yml`
4. Запустить `docker compose up -d`
5. Настроить мониторинг и алерты

---

## 🔧 УСТРАНЕНИЕ НЕПОЛАДОК

### Не могу подключиться через Cursor
**Проблема:** Connection timeout

**Решения:**
1. Проверьте что сервер запущен:
   ```powershell
   gcloud compute instances list
   ```

2. Проверьте SSH config:
   ```powershell
   cat C:\Users\konfu\.ssh\config
   ```

3. Попробуйте другой вариант подключения (sigmatrade vs sigmatrade-direct)

4. Проверьте путь к gcloud.cmd:
   ```powershell
   where.exe gcloud
   ```

### Docker не работает
**Проблема:** Permission denied

**Решение:**
```bash
# Переподключитесь к серверу (нужно перезайти для применения групп)
exit
ssh sigmatrade

# Проверьте группы
groups
# Должно быть: konfu adm dip video plugdev google-sudoers docker lxd
```

### Нет места на диске
**Проблема:** No space left

**Решение:**
```bash
# Очистка Docker
docker system prune -a --volumes

# Проверка места
df -h

# Удаление старых логов
sudo journalctl --vacuum-time=7d
```

---

## 📞 КОНТАКТЫ И ПОМОЩЬ

**Документация:**
- [Google Cloud SSH](https://cloud.google.com/compute/docs/instances/connecting-to-instance)
- [Docker документация](https://docs.docker.com/)
- [Docker Compose](https://docs.docker.com/compose/)

**Мониторинг:**
- [Консоль GCP](https://console.cloud.google.com/compute/instances)
- [Логи сервера](https://console.cloud.google.com/logs)

---

## 🎯 ИТОГО

### ✅ Готово к работе:
- Сервер запущен и доступен
- Docker установлен и настроен
- SSH подключение работает
- Структура директорий создана
- Cursor IDE готов к подключению

### ⏳ Ожидается:
- Код бота от пользователя
- Настройка docker-compose.yml
- Конфигурация переменных окружения
- Первый запуск и тестирование

---

**Дата создания:** 2025-11-10  
**Автор:** Claude AI Assistant  
**Версия:** 1.0

